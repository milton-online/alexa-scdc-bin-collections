{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
      {
        "label": "Start local dynamodb",
        "type": "shell",
        "command": "docker-compose up",
        "group": "test",
        "options": {
            "cwd": "${workspaceFolder}/dynamodb-local"
        },
        "presentation": {
          "reveal": "always",
          "panel": "new"
        }
      }
    ]
  }